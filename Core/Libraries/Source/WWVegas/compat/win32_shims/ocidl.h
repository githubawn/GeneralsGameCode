// TheSuperHackers @build bobtista 13/06/2026 Minimal <ocidl.h> shim. The OLE
// control interfaces it declares are only used by the Windows-only embedded
// web browser, which is stubbed out on non-Windows.
#pragma once

#include <objbase.h>
