# Cross-platform deterministic simulation-math CRC reference (macOS/arm64 + Windows x64).
#
# Produced by `generalszh -mathCrcCheck` (writes SimulationMathCrc.txt and prints to stdout).
# These are the values shared by macOS/arm64 and Windows x64/SSE2 (native IEEE-754 float/double,
# round-to-nearest, no FMA contraction).
#
# IMPORTANT CAVEAT (learned 14/06/2026): this probe is NOT a reliable predictor of full-sim
# cross-platform determinism. 32-bit x86 with the x87 _PC_24 clamp emits a DIFFERENT value here
# (85594C58/7EB5D952), yet replay testing shows that _PC_24 win32 build actually tracks an
# x64-recorded replay to x64's own self-desync floor (~frame 2600), while a win32 build WITHOUT
# _PC_24 matches this probe's value but desyncs from the real sim early (~frame 200). So a probe
# match does not imply lockstep, and a probe mismatch does not imply divergence. Treat REPLAY
# parity as the source of truth; use this probe only as a quick smoke signal for the IEEE arches.
#
# Captured: macOS arm64, build macos-generalsmd-sdl3-bgfx (Release), 14/06/2026.

SimulationMathCrc = 22F6E8A3
SimulationMathCrcDouble = FBAC7656
